services:
  insta-downloader:
    build: .
    ports:
      - "${PORT:-1905}:${PORT:-1905}"
    environment:
      - PORT=${PORT:-1905}
    volumes:
      - ./cookies.txt:/app/cookies.txt:ro
      - ./downloads:/app/downloads
    restart: unless-stopped
